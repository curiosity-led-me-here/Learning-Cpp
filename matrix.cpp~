#include <iostream>
#include <vector>

```cpp
class Matrix
{
    std::vector<std::vector<double>> data;
    Matrix(std::vector<std::vector<double>> values): data(values)
    
}
```

class A
{
    std::vector<std::vector<double>> B;
    A(std::vector<std::vector<double>> C)
    {
	B = C
    }
    
}
